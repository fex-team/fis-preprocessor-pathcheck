## FIS 路径检查插件

标准处理前，检查路径。无效路径报warning。

### 使用

```shell
$ npm install -g fis-preprocessor-pathCheck
$ vi path/to/project/fis-conf.js
```
```javascript
//configure plugin
fis.config.get('modules.preprocessor.js', 'jshint');


//configure plugin settings
fis.cofnig.set('settings.lipreprocessor.jshint', {
    //ignored some files
    //ignored : 'static/libs/**.js',
    ignored : [ 'static/libs/**.js', /jquery\.js$/i ]
});
```